services:
  sticker-vps:
    build: .
    ports:
      - "${PORT:-28749}:28749"
    environment:
      - PASSWORD=${PASSWORD}
      - PORT=28749
      - DATA_DIR=/data
    volumes:
      - ./data:/data
    restart: unless-stopped
